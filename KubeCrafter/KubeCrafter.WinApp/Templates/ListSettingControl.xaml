<UserControl x:Class="KubeCrafter.WinApp.Templates.ListSettingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KubeCrafter.WinApp.Templates"
             mc:Ignorable="d" MinWidth="0" MinHeight="0"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Top"
             HorizontalContentAlignment="Stretch"
             VerticalContentAlignment="Stretch"
             d:DesignHeight="200" d:DesignWidth="260">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{Binding Label}" FontWeight="Bold" Margin="0,0,0,5" Style="{StaticResource BasicTextBlock}"/>

                <ScrollViewer MinHeight="20" Grid.Row="1"  HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="ItemsPanel"/>
                </ScrollViewer>

        <Button Grid.Row="2" Content="Přidat položku" Margin="0,5,0,0" Click="AddItem_Click" Style="{StaticResource BasicButton}"/>
    </Grid>
</UserControl>
